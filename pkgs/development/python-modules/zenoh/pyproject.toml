[project]
name = "zenoh-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Guilhem Saurel", email = "guilhem.saurel@laas.fr" }
]
requires-python = ">=3.14"
dependencies = [
    "eclipse-zenoh>=1.10.0",
]

[project.scripts]
zenoh-test = "zenoh_test:main"

[build-system]
requires = ["uv_build>=0.11.28,<0.13.0"]
build-backend = "uv_build"
